<%= form_tag(Rails.application.routes.url_helpers.import_results_path(current_participatory_process, current_feature), multipart: true, class: "form new_import") do %>
  <div class="card">
    <div class="card-divider">
      <h2 class="card-title">
        <%= t(".title") %>
      </h2>
    </div>

    <% unless @errors.empty? %>
      <div class="card-section">
        <div class="row column">
          <table>
            <thead>
              <tr>
                <th><%= t(".row_number") %></th>
                <th><%= t(".errors") %></th>
              </tr>
            </thead>
            <% @errors.each do |error| %>
              <tr>
                <td><%= error.first %></td>
                <td>
                  <ul>
                    <% error.last.each do |error_message| %>
                      <li><%= error_message %></li>
                    <% end %>
                  </ul>
                </td>
              </tr>
            <% end %>
          </table>
        </div>
      </div>
    <% end %>

    <div class="card-section">
      <div class="row column">
        <%= file_field_tag :csv_file %>
      </div>
    </div>
  </div>

  <div class="button--double form-general-submit">
    <%= submit_tag t(".import"), class: "button" %>
  </div>
<% end %>